<div class="jumbotron jumbotron-fluid">
  <div class="container">
    <h1 class="display-3 text-light"><strong>Dashboard de <br><%= @user.first_name %> <%= @user.last_name %> </strong><br></h1>
  </div>
</div>

	<div class="container">
	  Identité : <%= @user.first_name %> <%= @user.last_name %></br> 
	  Mail : <%= @user.email %></br> 
	  Date de création du profil: <%= (@user.created_at).day %>/ <%= (@user.created_at).month %>/ <%= (@user.created_at).year%>
	</div>
