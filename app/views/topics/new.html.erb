<h1>Nouveau sujet</h1>

<%= form_for @topic do |f| %>
    <div class="form-group">
        <%= f.label :title, "Domaine" %><br />
        <%= f.collection_select :domain_id, Domain.all, :id, :name, prompt: 'Quel est le domaine ?', class: "form-control", :required => true %>
    </div>
	<div class="form-group">
		<%= f.text_field :name, placeholder:'Nom ', class: 'form-control' %>
	</div>
	<div class="form-group">
		<button class="btn btn-primary" type="submit">Créer le sujet</button>
	</div>

<% end %>



